import { expect } from 'chai'
import * as chasqui from './../../src/transport/chasqui.js'

describe('transport.chasqui', function () {

  describe('transport.chasqui.URIHandlerSend', function () {
    before(function () {  })

    it('Correctly polls for data', (done) => {
      expect(true, 'uport.network.id').to.equal(true)
    })
  })

  describe('transport.chasqui.poll', function () {
    before(function () {  })

    it('Correctly polls for data', (done) => {
      expect(true, 'uport.network.id').to.equal(true)
    })
  })

  describe('transport.chasqui.clearResponse', function () {
    before(function () {  })

    it('Correctly polls for data', (done) => {
      expect(true, 'uport.network.id').to.equal(true)
    })
  })
})
